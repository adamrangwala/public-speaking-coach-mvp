# nixpacks.toml

[phases.setup]
# Install ffmpeg system package
aptPkgs = ["ffmpeg"]

[start]
# Use the same start command as in railway.json
command = "PYTHONPATH=. python setup_database.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"